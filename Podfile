# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Tracker Jawn' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tracker Jawn
  pod 'Charts'

  target 'Tracker JawnTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tracker JawnUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
